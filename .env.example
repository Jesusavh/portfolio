# Cloudinary (público). Reemplaza `vhqilnly` por tu cloud name real:
# Dashboard → Account Details → Cloud name.
# Si no defines esta variable, el build usa `vhqilnly` por defecto.
PUBLIC_CLOUDINARY_CLOUD_NAME=vhqilnly
